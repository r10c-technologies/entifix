'use client';

import { invoiceCrud } from '../../../../cruds';

/** The same generated screen as the workspace tab, hosted by a route. */
export default function SingleViewPage() {
  return <invoiceCrud.SingleViewPage />;
}
