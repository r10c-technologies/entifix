import { buildEntityBaseUrl } from './build-entity-base-url.js';
import { buildEntityRequest } from './build-entity-request.js';

export const buildEntityRestAdapterMixins = {
  buildEntityBaseUrl,
  buildEntityRequest,
};
